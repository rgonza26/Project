<h1>Welcome to your Profile</h1>

<%= image_tag 'profilepic.png', :alt => 'profilepic', :align => 'left',  :width => 90, :height => 100 =%> 
<h2> <%=''=%>  User</h2>
<BR>
<BR>
<BR>
<BR>
<BR>
<BR>
<h2>Booked Events</h2>	
<div class="media">
  <div class="media-body">
    <table class="table table-hover">
      <thead>
        <tr>
          <div class="container">
            <ul class="nav events">
              <th>Name</th>
              <th>Date</th>
              <th>Description</th>
              <th>Image</th>
              <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @bookings.each do |booking| %>
        	<% @mainevents.each do |mainevent| %>
       			<% if booking.eventid == mainevent.id %>
		          <tr>
		            <td><%= mainevent.name %></td>
		            <td><%= mainevent.date %></td>
		            <td><%= mainevent.description %></td>
		            <td><%= image_tag mainevent.image %></td>
		            <td><%= link_to 'Show', mainevent %></td>
            <% end %>
         <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
