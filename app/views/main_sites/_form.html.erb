<%= form_for(@main_site) do |f| %>
  <% if @main_site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@main_site.errors.count, "error") %> prohibited this main_site from being saved:</h2>

      <ul>
      <% @main_site.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
